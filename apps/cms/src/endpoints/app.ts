import { endpoints as searchEndpoints } from "./search";
import { endpoints as pressReleaseEndpoints } from "./press-releases";
// For non collection-specific endpoints only
export default [...searchEndpoints];
